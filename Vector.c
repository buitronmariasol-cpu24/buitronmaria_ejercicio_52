#include <stdio.h>

int main ()
{

   int n, i, A[50];

   printf("Ingrese el numero de elementos al vestor A")
   scanf("%d", &n);

   for(i=0; i < n; i++)
   {

    printf("Ingrese A[%d]:", i);
    scanf("%d", &A[i]:"

   }
   return 0;
}
